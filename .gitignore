# Environment
.env
.env.local
.env.production

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Database (persisted history — runtime data, not source)
backend/data/
*.db
*.db-journal

# Secrets (accidentally placed outside .env)
*access token*
*.token

# Local tunneling (ngrok demo sessions)
ngrok_log.txt

# Dev server scratch logs (background test runs during sessions)
backend/uvicorn_test*.log
backend/uvicorn_local*.log
frontend/vite_test.log

# Pwn Lab demo scratch workspace (compiled binaries + downloaded challenge sources,
# not project source — regenerated on demand via the app's own API)
pwnlab-workspace/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
